name: Temporary Support Final Code Patch

on:
  push:
    branches:
      - support/manager-agent-workflow
    paths:
      - .github/workflows/temp-support-final-code-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: support/manager-agent-workflow
          fetch-depth: 0
      - run: python3 tools/temp_support_final_code_patch.py
      - run: |
          rm tools/temp_support_final_code_patch.py
          rm .github/workflows/temp-support-final-code-patch.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix final support workflow checks"
          git push origin HEAD:support/manager-agent-workflow

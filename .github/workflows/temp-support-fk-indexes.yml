name: Temporary Support FK Indexes

on:
  push:
    branches:
      - support/manager-agent-workflow
    paths:
      - .github/workflows/temp-support-fk-indexes.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: support/manager-agent-workflow
          fetch-depth: 0
      - run: python3 tools/temp_support_fk_indexes.py
      - run: |
          rm tools/temp_support_fk_indexes.py
          rm .github/workflows/temp-support-fk-indexes.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Index support workflow foreign keys"
          git push origin HEAD:support/manager-agent-workflow

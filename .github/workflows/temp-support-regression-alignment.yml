name: Temporary Support Regression Alignment

on:
  push:
    branches:
      - support/manager-agent-workflow
    paths:
      - .github/workflows/temp-support-regression-alignment.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: support/manager-agent-workflow
          fetch-depth: 0
      - run: python3 tools/temp_support_regression_alignment.py
      - run: |
          rm tools/temp_support_regression_alignment.py
          rm .github/workflows/temp-support-regression-alignment.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Align support manager regression with execution model"
          git push origin HEAD:support/manager-agent-workflow
